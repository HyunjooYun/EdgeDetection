# Next Steps (2025-12-10)

## 1. PPO 학습 스크립트 구현
- `scripts/train_ppo.py` 초안 작성: DQN 스크립트 구조를 재사용해 CLI 옵션(모델 경로, TensorBoard 로그, 아티팩트 저장 경로 등)을 일치시킵니다.
- Stable-Baselines3 `PPO`를 사용해 동일한 HED 환경 구성을 로드하고, 파라미터 세팅(learning rate, batch size, n_steps 등)을 합리적인 기본값으로 지정합니다.
- 짧은 스텝(예: 2,000)으로 스모크 테스트를 실행해, 로그와 모델이 정상 생성되는지 확인합니다.

## 2. DQN vs PPO 비교 실험 설정
- 공통 입력/환경 설정을 모듈화하거나 유틸 함수로 분리하여 중복 코드를 줄입니다.
- TensorBoard 로그 디렉터리를 분리하여(`runs/dqn_*`, `runs/ppo_*`) 동시에 실행해도 기록이 섞이지 않도록 구성합니다.
- 핵심 비교 지표: 평균 에피소드 보상, 에피소드 길이, 탐색률(해당 시), 학습 손실을 추적·시각화합니다.
- 장기 학습(수만 스텝 이상)을 위한 실행 계획(시간 확보, 하드웨어 자원, 로그 저장 공간)을 마련합니다.

## 3. 추가 고려 사항
- TensorBoard 실시간 확인과 Matplotlib 기반 정적 플롯 모두 지원해 결과 보고서를 쉽게 작성할 수 있게 합니다.
- 대용량 모델 파일(`hed_pretrained_bsds.caffemodel`)은 Git LFS로 이전하는 것도 검토합니다.
- 필요 시 실험 자동화를 위해 간단한 배치 스크립트 혹은 Makefile 대안을 작성합니다.
