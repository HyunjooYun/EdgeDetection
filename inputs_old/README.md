# Dataset Placement

- Put training RGB images in `inputs/train/`.
- Put the matching ground-truth edge maps in `inputs/GT/` with identical filenames (e.g., `12003.jpg` ↔ `12003.png`).
- These folders are ignored by Git so datasets are not checked in; keep backups separately if needed.
